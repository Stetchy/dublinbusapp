class pointOfInterest{
  constructor(name, longitude, latitude, icon) {
    this.name = name
    this.longitude = longitude;
    this.latitude = latitude;
    this.icon = icon
  }
}
export default pointOfInterest;
